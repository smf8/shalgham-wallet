apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wallet.fullname" . }}-test-connection"
  labels:
    {{- include "wallet.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wallet.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
